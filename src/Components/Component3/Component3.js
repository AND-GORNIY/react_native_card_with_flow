//@flow
import React from 'react';
import {Text} from 'react-native';

type Props = {type: string};

const Component3 = ({type}: Props) => {
  return <Text>{type}</Text>;
};

export default Component3;
